apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: ack-lambda-controller
  labels:
    app.kubernetes.io/name: ack-lambda-controller
    app.kubernetes.io/instance: ack-lambda-controller
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: ack-lambda-controller
  podMetricsEndpoints:
    - path: /metrics
      port: http
